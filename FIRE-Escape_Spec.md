# アプリケーション製品仕様書：『FIRE Escape』 v1.0

> **対象プラットフォーム：Expo (React Native) ネイティブ優先**

---

## 1. 概要

本アプリは、「今すぐ会社を辞めた場合、現在の資産と生活費で何年生きられるか（あるいは一生逃げ切れるか）」を秒単位で可視化する、会社員のためのエスケープ（脱出）シミュレーターである。真面目な資産管理アプリの皮を被った「脳汁分泌型ライフタイマー」を目指す。

- **正式名称：** FIRE Escape - 会社を辞めたら何年生きられる？
- **開発思想：** アカウント登録不要、サーバー不要（完全ローカル計算）。開いて10秒で本質的な体験を届ける。初期バージョンはマネタイズを一切考慮せず、シェアしたくなる体験の磨き込みに特化する。
- **プラットフォーム：** Expo (React Native)。iOS / Android ネイティブ。

### 1.1. このアプリが勝つための1点

このアプリのすべては「**スライダーを動かした瞬間に、タイマーが伸縮し、指先が微振動する**」という一瞬の快感に集約される。ここが凡庸だと全てが死ぬ。逆にここさえ完璧なら、機能が少なくても勝てる。実装リソースはこの1点に最優先で投下する。

---

## 2. デザインシステム & ブランドアイデンティティ

- **世界観：** 暗闇の中の「非常口（エスケープ）」。サイバーパンクかつミニマルな高級感。安っぽいネオンではなく、漆黒に浮かぶ精密な計器のような質感を目指す。
- **カラーパレット：**

| 役割 | 変数名 | カラーコード | 用途 |
|---|---|---|---|
| Background | `--bg` | `#050505` | 漆黒の背景 |
| Card / Panel | `--panel` | `#111111` | パネル・カード |
| Accent 1 (炎/警告) | `--fire` | `#FF4500` | 資産減少時・入力中・カウントダウン |
| Accent 2 (脱出/安全) | `--escape` | `#00E676` | FIRE達成時・非常口カラー |
| Text Primary | `--text` | `#FFFFFF` | 数字・主要テキスト |
| Text Muted | `--muted` | `#888888` | 補助テキスト・初期状態 |

- **フォント（数字）：** `JetBrains Mono`（等幅）。カウントダウンの数字が動いても桁がガタつかないために**等幅は絶対条件**。Expoでは `expo-font` で同梱する。
- **フォント（UI）：** 数字はMono、ラベルやコピーは別の表情のあるサンセリフで対比をつける（候補：`Archivo` / `Hubot Sans` など。生成AIっぽさを避けるため Inter / Roboto は使わない）。
- **質感の指針：** 単色ベタ塗りを避ける。漆黒の背景にごく微細なノイズ／グレイン、Accent発光部には軽いglow（ネイティブなら `shadowColor` + blur）を乗せ、計器が光っているような奥行きを出す。

---

## 3. 画面構成とユーザーフロー

基本は「**1画面完結型（シングルスクリーン）**」。スクロールなしで全機能が1画面に収まることを原則とする。

```
+-------------------------------------------------------+
|                       [ ロゴ ]                        |
|                                                       |
|          ◆◆◆  ESCAPE TIMER (結果表示)  ◆◆◆          |
|              XX年 XXヶ月 XX日 XX:XX:XX                 |
|               [ 称号・ステータスタグ ]                |
|                                                       |
+-------------------------------------------------------+
|          ■■■  INPUT PANEL (操作エリア)  ■■■          |
|                                                       |
|  1. 現在の総資産        [ 800万円 ]                   |
|  [======================o                            ]|
|  2. 毎月の生活費        [  25万円 ]                   |
|  [=========o                                         ]|
|  3. 退職後のミニマム収入 [   5万円 ]                  |
|  [====o                                              ]|
|  4. 想定運用利回り      [  4.0% ]                     |
|  [=======o                                           ]|
|                                                       |
+-------------------------------------------------------+
|                 [ SHARE ON X ] ボタン                 |
+-------------------------------------------------------+
```

### 3.1. 初期状態（静寂）

- アプリ起動時、タイマー部分は `--年 --ヶ月 --日 --:--:--` と `--muted` でグレーアウト。
- 中央に大きく「**ESCAPE（辞表を出す）**」という `--fire` のボタンが鎮座。スライダーは初期値（資産800万・生活25万・副収入5万・利回り4.0%）に設定済みだが、結果はまだ「封印」されている。
- この「結果を見るには覚悟が要る」という静寂が、次の起動演出のカタルシスを生む。

### 3.2. 起動演出（脳汁ポイント）

ユーザーが「ESCAPE」ボタンをタップした瞬間からの演出シーケンス（§7 にミリ秒タイミング表）：

1. `Haptics.notificationAsync(Heavy)` 相当の「ドクン」が1回。
2. グレーアウトしていたタイマーが `--escape` または `--fire`（結果に応じた色）にフェードイン発光。
3. デフォルト値での計算結果に向かって、数字が**爆速でカウントアップ**（ease-out で減速）。
4. 最終的な「自由時間」でピタッと停止し、軽い `Haptics.impactAsync(Light)` で着地感。
5. そこから1秒ずつのリアルタイムカウントダウンが始まる。

---

## 4. 機能要件

### 4.1. 4つの入力パラメーター（UI: スライダー）

入力はスライダー操作のみ。数値を動かした瞬間、`Haptics.selectionAsync()` 相当の微振動（Tick）が走り、上部タイマーがリアルタイムに伸縮する。

| # | パラメーター | 範囲 | ステップ | 初期値 |
|---|---|---|---|---|
| 1 | 現在の総資産（貯金・NISA等すべて） | 0円 〜 1億円 | 50万円 | 800万円 |
| 2 | 毎月の生活費 | 5万円 〜 100万円 | 1万円 | 25万円 |
| 3 | 退職後のミニマム収入（希望枠） | 0万円 〜 50万円 | 1万円 | 5万円 |
| 4 | 想定運用利回り（年利） | 0.0% 〜 10.0% | 0.1% | 4.0% |

> **#3 の希望枠について：** 「会社は辞めるけど、週2バイトや副業でこれくらいは稼ぐ」という現実的な妥協ライン。これを入れると一気に生きられる年数が伸び、INFINITE FREEDOM への質的転換を起こす唯一のスイッチでもある。**体験のピークになるスライダーなので、最も気持ちよく動くよう最優先でチューニングする。**

#### 4.1.1. Tickフィードバックのスロットリング（実装注意）

スライダーのドラッグ中、値が変わるたびに `selectionAsync()` を呼ぶと、ハイレートなドラッグで振動がモーター追従しきれず「ベター」と潰れる。**1ステップ進むごとに1回**だけ発火するようスロットリングする（前回値と比較し、ステップ境界をまたいだ時のみ発火）。ここの実装品質がアプリの体感を決める。

### 4.2. コア計算ロジック（複利取り崩し）— 純粋関数として実装

以下を**プラットフォーム非依存の純粋関数**として最初に実装し、UIより先にテストを通す。

#### 計算の定義

- 毎月の純減額 `netDraw = monthlyCost − sideIncome`
- 毎月の処理順序を以下に固定する（順序を変えると秒カウントがズレるため厳守）：
  1. 残高に運用益を加算：`balance += balance × (annualRate / 12)`
  2. 純減額を引く：`balance −= netDraw`
  3. 経過月数を +1
  4. `balance <= 0` になったらその月で枯渇

#### 状態の3分類

| 状態 | 判定条件 | 表示 | 色 |
|---|---|---|---|
| **TRUE_INFINITE（真の無限）** | 初月運用益 `assets × (rate/12)` ≥ `netDraw` | `INFINITE FREEDOM` | `--escape` 発光 |
| **PRACTICAL_INFINITE（実質無限）** | 上記に当てはまらず、かつ枯渇まで100年（1200ヶ月）超 | `99年+ 逃げ切り濃厚` | `--escape` ほのかに |
| **DEPLETE（枯渇）** | 1200ヶ月以内に `balance <= 0` | カウントダウン表示 | `--fire` |

> TRUE_INFINITE は「運用益だけで生活費を賄えている＝資産が減らない・むしろ増える」状態で、これこそが緑発光に値する「完全脱獄」。単に長持ちするだけの PRACTICAL_INFINITE と区別することで、緑発光の感動を希薄化させない。

#### 関数シグネチャ（TypeScript）

```typescript
type EscapeStatus = 'TRUE_INFINITE' | 'PRACTICAL_INFINITE' | 'DEPLETE';

interface EscapeResult {
  status: EscapeStatus;
  months: number | null;   // DEPLETE のときのみ枯渇までの月数。INFINITE系は null
}

function calcEscape(
  assets: number,        // 総資産(円)
  monthlyCost: number,   // 毎月の生活費(円)
  sideIncome: number,    // 退職後ミニマム収入(円/月)
  annualRate: number,    // 年利(小数: 0.04 = 4%)
): EscapeResult
```

#### 確定テストケース（実装前にこれを固定する）

以下は検証済みの確定値。

| ケース | 資産 | 生活費 | 副収入 | 利回り | 期待結果 |
|---|---|---|---|---|---|
| デフォルト値 | 8,000,000 | 250,000 | 50,000 | 4.0% | DEPLETE / 44ヶ月 (3年8ヶ月) |
| 副収入ゼロ | 8,000,000 | 250,000 | 0 | 4.0% | DEPLETE / 34ヶ月 (2年10ヶ月) |
| 利回りゼロ | 8,000,000 | 250,000 | 50,000 | 0.0% | DEPLETE / 40ヶ月 (3年4ヶ月) |
| 即破産級 | 500,000 | 250,000 | 0 | 0.0% | DEPLETE / 2ヶ月 |
| 高資産・無限 | 100,000,000 | 250,000 | 0 | 4.0% | TRUE_INFINITE |
| 副収入で逃げ切り | 5,000,000 | 200,000 | 200,000 | 0.0% | TRUE_INFINITE |
| INFINITE境界ちょうど | 60,000,000 | 250,000 | 50,000 | 4.0% | TRUE_INFINITE |
| 境界ぎりぎり手前 | 59,990,000 | 250,000 | 50,000 | 4.0% | DEPLETE（巨大な月数） |
| 長持ちだが運用益不足 | 5,000,000 | 200,000 | 199,000 | 0.0% | PRACTICAL_INFINITE（416年相当→100年で頭打ち） |
| 資産1億・生活50万・利回り0 | 100,000,000 | 500,000 | 0 | 0.0% | DEPLETE / 200ヶ月 (16年8ヶ月) |

> **INFINITE境界の検算：** 純減20万/月・利回り4%なら、運用益が純減と釣り合うのは資産6,000万円ちょうど（6,000万 × 0.04 ÷ 12 = 20万）。6,000万でTRUE_INFINITE、5,999万でDEPLETE。境界テストは必ず両側を含めること。

#### 設計上の明示的判断：インフレ調整は「入れない」

生活費が年々上がるインフレ調整は**あえて実装しない**。リアルにすると夢がなくなり、シェアの価値（FIRE達成のカタルシス）が削がれるため。これは精度を犠牲にした意図的な設計判断であり、コメントに明記しておく。

### 4.3. 動的ステータスタグ（称号）

計算された「生きられる年数」に応じて、画面中央に称号をリアルタイム表示。

| 生きられる年数 | 称号（タグ） | カラー |
|---|---|---|
| 0年（即破産） | `明日も出社確定（ゾンビ）` | `--muted` |
| 1年未満 | `刹那的自由（来年ニート）` | `--fire` |
| 1年以上〜3年未満 | `限定的モラトリアム` | オレンジ |
| 3年以上〜10年未満 | `中長期的マベリック` | 黄 |
| 10年以上〜30年未満 | `半永久的エスケープ成功` | 青 |
| 30年以上 / TRUE_INFINITE | `監獄からの完全脱獄（FIRE）` | `--escape` |

### 4.4. 「希望枠」の煽り設計

INFINITE FREEDOM 達成の**手前**にいるとき、タイマー下に小さく煽りテキストを出す。これが「希望枠」スライダーを最後のひと押しさせる仕掛け。

- DEPLETEだが TRUE_INFINITE まで近い（必要追加副収入が月5万円以内など）とき：
  「**あと月◯万円稼げれば、一生自由。**」
- ◯には `TRUE_INFINITE 達成に必要な追加副収入額`（= netDraw − assets×rate/12 の不足分）を1万円単位で表示。
- 達成した瞬間、テキストが消えて画面全体が `--escape` に発光転換 → ここで `Haptics.notificationAsync(Success)`。

> この「もうちょっとで緑になる」の可視化が、スライダーをいじり続けさせる中毒性の核。

### 4.5. SNSシェア機能（バイラルハック）

「Xでシェア」ボタンで、結果画面を1枚に美しくレンダリングした画像を生成し、シェアシートを開く。

- **画像生成：** `react-native-view-shot` で結果カードのViewをキャプチャ → `expo-sharing` でシェアシート起動（X/Instagram Stories/LINE等、ユーザーが選べる）。
- **画像レイアウト：** §9.3 のワイヤー参照。中央に大きく「私のFIRE Escape：あと12年4ヶ月生きられます」、下部に `#FIRE_Escape` と、後述のランディングURLを焼き込む。
- **インストールの壁への対策：** ネイティブアプリは「面白い結果→友達に送る→友達もその場で試す」のループが、ストアのインストールの壁で切れる。対策として：
  1. シェア画像に**短いランディングURL**（例：`fire-escape.app`）を焼き込み、そこにWeb簡易版 or ストアリンクを置く。
  2. 可能なら**軽量なWeb版（読み取り専用 or 簡易計算のみ）を別途用意**し、画像を見た人がブラウザで即体験 → 気に入ったらアプリへ、という二段構えにする。これは初期リリース後の課題として切り出してよいが、設計上の穴として認識しておく。

---

## 5. 技術スタック

| 領域 | 採用 | 備考 |
|---|---|---|
| フレームワーク | Expo (React Native) | Expo SDK 最新安定版 |
| 言語 | TypeScript | 計算ロジックは厳格な型で |
| ハプティック | `expo-haptics` | Tick / 着地 / 成功 を使い分け |
| フォント | `expo-font` | JetBrains Mono 同梱 |
| スライダー | `@react-native-community/slider` または Reanimated 自作 | 体感重視なら自作も検討 |
| アニメーション | `react-native-reanimated` | 60fpsのタイマー伸縮・カウント |
| 画像キャプチャ | `react-native-view-shot` | シェア画像生成 |
| シェア | `expo-sharing` | OSのシェアシート |
| 状態管理 | ローカルstate（zustand等は任意） | サーバー・認証なし |

---

## 6. 開発ステップ

| Day | ステップ | ゴール |
|---|---|---|
| Day 1 | **計算ロジックの確立** | §4.2 の `calcEscape` を純粋関数で実装し、§4.2の確定テストを全通過。UIはまだ触らない。 |
| Day 2 | **爆速タイマーUI** | 1秒ごとに減算（または増加）するカウントダウンコンポーネント。Mono固定で桁ガタつきゼロ。 |
| Day 3 | **スライダー × ハプティック連動** | ダークモードの仕上げ。スライダーとタイマー伸縮を完全同期。Tickのスロットリング（§4.1.1）を実機でチューニング。 |
| Day 4-5 | **起動演出・煽り・シェア画像** | ESCAPEボタンの「ドクン」、希望枠の煽りテキスト、view-shotでのシェア画像生成。デザイン最終調整 → リリース。 |

---

## 7. アニメーション・タイミング表（ミリ秒）

| イベント | 内容 | 時間 | イージング |
|---|---|---|---|
| ESCAPE押下 | Heavy haptic「ドクン」 | 0ms | — |
| タイマー発光 | グレー→結果色へフェードイン | 0–300ms | ease-in |
| カウントアップ | 0 → 結果値へ爆速回転 | 300–1500ms | ease-out（終盤で減速） |
| 着地 | 結果値で停止 + Light haptic + 軽いバウンス | 1500–1700ms | spring |
| リアルタイム減算開始 | 1秒ごとのカウントダウン | 1700ms〜 | linear |
| スライダーTick | 1ステップごとにselection haptic | ドラッグ中 | — |
| タイマー伸縮 | スライダー値変化に追従 | 即時（<100ms） | ease-out |
| INFINITE転換 | 全体が緑に発光 + Success haptic | 達成時 250ms | ease-in-out |

---

## 8. やらないこと（スコープ外・初期版）

体験の磨き込みに集中するため、初期版では以下を**あえてやらない**。

- アカウント登録・ログイン・サーバー保存
- インフレ調整・税金・年金などの精緻なFP計算（§4.2の判断）
- 複数シナリオの保存・比較
- 課金・広告（マネタイズは体験が証明されてから）
- 多言語対応（まず日本語のみ）

---

## 9. 別添：先行作成素材

### 9.1. デザイントークン（`tokens.ts` として渡す）

```typescript
export const colors = {
  bg: '#050505',
  panel: '#111111',
  fire: '#FF4500',
  escape: '#00E676',
  text: '#FFFFFF',
  muted: '#888888',
} as const;

export const tiers = [
  { maxYears: 0,        label: '明日も出社確定（ゾンビ）',     color: '#888888' },
  { maxYears: 1,        label: '刹那的自由（来年ニート）',     color: '#FF4500' },
  { maxYears: 3,        label: '限定的モラトリアム',           color: '#FF8C00' },
  { maxYears: 10,       label: '中長期的マベリック',           color: '#FFD400' },
  { maxYears: 30,       label: '半永久的エスケープ成功',       color: '#4FA3FF' },
  { maxYears: Infinity, label: '監獄からの完全脱獄（FIRE）',   color: '#00E676' },
] as const;

export const defaults = {
  assets: 8_000_000,
  monthlyCost: 250_000,
  sideIncome: 50_000,
  annualRate: 0.04,
} as const;
```

### 9.2. 称号コピー（確定テキスト・後から100回いじる前提で別管理）

§4.3 の表参照。文言は体験の面白さの大部分を握るので、`tiers` 配列の `label` を直書きさせず必ずトークン化して渡す。

### 9.3. シェア画像レイアウト（ワイヤー）

```
+-----------------------------------+
|  FIRE Escape          [非常口アイコン]|
|                                   |
|        私のFIRE Escape            |
|                                   |
|      あと 12年 4ヶ月 生きられます   |   ← 最大級フォント、結果色
|                                   |
|     [ 監獄からの完全脱獄（FIRE） ]   |   ← 称号タグ
|                                   |
|  資産800万 / 生活25万 / 副収入5万   |   ← 小さく入力サマリ
|                                   |
|  #FIRE_Escape    fire-escape.app  |   ← ハッシュタグ + LP URL
+-----------------------------------+
```

- 背景は `--bg`、結果数値は状態に応じて `--fire` / `--escape`。
- 画像比率はX/Instagram双方で映える正方形（1080×1080）を基本に、Stories用に縦長（1080×1920）の派生も将来検討。
