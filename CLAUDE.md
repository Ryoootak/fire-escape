# CLAUDE.md — FIRE Escape

このファイルは Claude Code がこのリポジトリで作業する際に毎回読み込む常駐ルール。
「何を作るか」は `FIRE_Escape_Spec.md`（製品仕様書）を正とする。このファイルは「どう作るか（進め方・規律）」を定める。

---

## プロジェクト概要

- **アプリ名：** FIRE Escape — 会社を辞めたら何年生きられるかを秒単位で可視化する脱出シミュレーター
- **プラットフォーム：** Expo (React Native) / iOS・Android ネイティブ
- **言語：** TypeScript
- **思想：** サーバーなし・認証なし・完全ローカル計算。マネタイズより「シェアしたくなる体験」の磨き込み最優先。
- **詳細仕様：** `FIRE_Escape_Spec.md` を参照。機能・計算ロジック・デザイン・開発ステップはすべてそちらが正。

---

## 進め方の規律（最重要）

- **仕様書のDayステップ単位で進める。** 指示された Day のスコープだけをやり、次の Day には勝手に進まない。各 Day の完了時は必ず止まって報告する。
- **計算ロジックはUIより先。** `src/lib/calcEscape.ts` を純粋関数として実装し、仕様書 §4.2 の確定テストを全通過させてから UI に着手する。テストが落ちている状態で UI を書き始めない。
- **大きく作り直すより、まず小さく動かす。** 一度に大量のファイルを生成せず、動く最小単位を積み上げる。

## Git / コミット規律

- **節目ごとに必ず commit する。** 具体的には「Expo初期化完了」「Day 1テスト全通過」「各コンポーネント完成」など、戻れる地点を意味のある単位で刻む。
- コミットメッセージは日本語でよい。何をしたかが一目で分かる粒度で書く。
- 破壊的な変更（大量のファイル削除・リネーム）の前には、直前にコミットがあることを確認する。なければ先にコミットしてから着手する。
- `node_modules`、ビルド成果物、`.env` 等はコミットしない（Expoの `.gitignore` に従う。不足があれば追記する）。

## コード規律

- **TypeScript は strict モード。** `any` は原則禁止。型で守る。
- 計算ロジック（`src/lib/` 配下）は副作用のない純粋関数として書き、UI から切り離してテスト可能にする。
- **デザイントークン（色・称号ラベル・初期値）は `src/lib/tokens.ts` に集約。** 色コードや称号文言をコンポーネントに直書きしない。これらは後から頻繁に調整するため。
- ファイルは1責務1ファイルを基本とし、肥大化したら分割する。

## このアプリの「品質の急所」（手を抜かない箇所）

- **スライダー操作時のハプティック（微振動）とタイマー伸縮の同期。** ここがこのアプリの体験の核。仕様書 §4.1.1 のTickスロットリングは実機での体感を見ながら丁寧にチューニングする。
- **等幅フォント（JetBrains Mono）固定で数字の桁ガタつきをゼロに。** カウントダウン中に数字幅が動くのは品質欠陥とみなす。
- **「希望枠」スライダー周りの煽り演出（仕様書 §4.4）とINFINITE転換の発光。** 体験のピーク。雑に実装しない。

## やらないこと（初期版スコープ外）

- 認証・ログイン・サーバー保存
- インフレ調整・税金・年金などの精緻なFP計算（意図的に省く設計判断。仕様書 §4.2 参照）
- 課金・広告・多言語対応

## 報告のしかた

- 各 Day / 各タスク完了時に、「何をしたか」「テスト結果」「次にやるべきこと」を簡潔に報告して、止まる。
- 仕様書の解釈に迷う点や、仕様の穴に気づいた場合は、勝手に判断で埋めずに質問する。
